{
  "version": 1,
  "last_updated": "2026-03-22",
  "canonical": [
    "BUILD_BRIEF.md",
    "docs/ROADMAP.md",
    "docs/ROADMAP_ANCHOR.md",
    "docs/ux/recall_benchmark_matrix.md",
    "docs/exec_plans/active/2026-03-22_stage497_repo_closeout_and_cleanup_after_stage496.md"
  ],
  "bridges": [
    "agent.md",
    "docs/assistant/INDEX.md",
    "docs/assistant/APP_KNOWLEDGE.md"
  ],
  "workflows": [
    {
      "id": "roadmap_continuation",
      "doc": "docs/assistant/workflows/ROADMAP_WORKFLOW.md",
      "scope": "Roadmap-driven product work and milestone continuation.",
      "primary_files": [
        "BUILD_BRIEF.md",
        "docs/ROADMAP.md",
        "docs/ROADMAP_ANCHOR.md",
        "docs/ux/recall_benchmark_matrix.md",
        "docs/exec_plans/active/2026-03-22_stage497_repo_closeout_and_cleanup_after_stage496.md"
      ],
      "validation_commands": [
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm test -- --run'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm run lint'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm run build'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/backend && .venv/bin/python -m pytest'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/backend && .venv/bin/python -c \"from app.main import app; print(app.title)\"'"
      ]
    },
    {
      "id": "edge_speech_validation",
      "doc": "docs/assistant/workflows/EDGE_SPEECH_VALIDATION_WORKFLOW.md",
      "scope": "Edge read-aloud, sentence highlighting, progress restore, and manual browser validation.",
      "primary_files": [
        "frontend/src/App.tsx",
        "frontend/src/hooks/useSpeech.ts",
        "frontend/src/components/ReaderSurface.tsx",
        "frontend/src/lib/segment.ts"
      ],
      "validation_commands": [
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm test -- --run'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm run lint'",
        "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm run build'"
      ]
    }
  ],
  "global_commands": {
    "repo_status": "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader && git status --short --branch'",
    "frontend_dev": "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/frontend && npm run dev -- --host 127.0.0.1'",
    "backend_dev": "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/backend && .venv/bin/python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000'",
    "backend_pytest": "wsl.exe bash -lc 'cd /home/fa507/dev/accessible_reader/backend && .venv/bin/python -m pytest'"
  },
  "contracts": {
    "template_read_policy": "Read docs/assistant/templates/* only when the user explicitly asks for harness/bootstrap prompt creation, cross-project Codex scaffolding, or a follow-up delta/refinement prompt.",
    "delta_refinement_prompt_policy": "When the user explicitly asks for a follow-up delta/refinement prompt based on later prototype or live-review learnings, use docs/assistant/templates/CODEX_DELTA_REFINEMENT_PROMPT.md and keep the pass inspect-first: keep good work, correct weak work, add only the meaningful delta.",
    "wsl_toolchain_home": "Run git, npm, pytest, and uvicorn from WSL against /home/fa507/dev/accessible_reader.",
    "browser_target": "Validate browser behavior in Windows Edge on Windows 11.",
    "local_first_default": "Keep parsing, storage, search, settings, progress, and deterministic reflow local-first.",
    "web_import_scope": "Public webpage import is limited to article-style pages, fetched once, stored locally as snapshots, and never live-synced on reopen.",
    "speech_path": "Browser-native speechSynthesis is the shipped read-aloud path for v1.",
    "local_tts_status": "Local TTS remains deferred and should be described as coming soon unless the user reprioritizes it.",
    "ai_scope": "AI is opt-in only and limited to Simplify and Summary.",
    "reader_generated_content_lock": "In the current Recall-parity track, Reader is original-only and cosmetic-only. Do not change Reflowed, Simplified, Summary, generated-view UX, transform logic, generated placeholders, generated-view controls, or mode-routing unless the user explicitly reprioritizes generated-content work.",
    "ux_priority": "Prefer the best Recall-quality workflow over preserving the current UI structure; preserve local-first behavior, routes, anchors, and reading continuity instead of freezing layout.",
    "validation_strategy": "Run targeted validation before broad checks; keep backend pytest targeted when backend code is untouched; for Recall shell or top-level UI work, require fresh screenshot artifacts, use targeted Vitest coverage first, and keep the broad App.test.tsx pass plus repo-owned Edge harnesses available for shell, route, and visual continuity checks. If App.test.tsx ever appears to stall again, inspect App-level callback identity churn and ReaderWorkspace effect dependencies before treating the file as an unreliable runner.",
    "push_policy": "Do not push unless the user explicitly asks.",
    "execplan_policy": "Major or multi-file work must start by creating or updating an ExecPlan in docs/exec_plans/active/.",
    "desktop_first_queue_policy": "Keep one active desktop-first milestone at a time. The Stage 380 audit reset the current Recall-parity track to Graph, Home, and original-only Reader; the Stage 382 audit closed the Graph reset pair; the Stage 384 audit closed the first Home reset pair; the Stage 386 audit closed the original-only Reader reset pair; the Stage 388 audit closed the second-pass Home density reset pair; the Stage 390 audit closed the second-pass Graph View 2.0 reset pair; the Stage 392 audit closed the broader Home tree-filtered reset pair; the Stage 394 audit closed the broader original-only Reader reading-space reset pair; the Stage 396 audit closed the broader Home tag-tree board reset pair; the Stage 398 audit closed the broader Home board-fill reset pair; the Stage 400 audit closed the broader Home filtered-card density reset pair; the Stage 402 audit closed the broader Home continuous-board-coverage reset pair; the Stage 404 audit closed the broader Home library-sheet flattening reset pair; the Stage 406 audit closed the broader Home tag-tree control-surface reset pair; the Stage 408 audit closed the broader Home minimal-entry reset pair; the Stage 410 audit closed the broader Home organizer-rail header reset pair; the Stage 412 audit closed the broader Home organizer-row flattening reset pair; the Stage 414 audit closed the broader Home organizer-selection continuity reset pair; the Stage 416 audit closed the broader Home organizer-highlight deflation reset pair; the Stage 418 audit closed the broader Home organizer-readout softening reset pair; the Stage 420 audit closed the broader Home organizer-preview grouping reset pair; the Stage 422 audit closed the broader Home summary-preview join reset pair; the Stage 424 audit closed the broader Home active summary-note integration reset pair; the Stage 426 audit closed the broader Home active summary-copy deflation reset pair; the Stage 428 audit closed the broader Home active bridge-hint retirement reset pair; the Stage 430 audit closed the broader Graph corner-controls and focus-rail reset pair; the Stage 432 audit closed the broader Home organizer-control deck reset pair; the Stage 434 audit closed the broader Graph canvas-first drawer reset pair; the Stage 436 audit closed the broader Home board-first organizer reset pair; the Stage 438 audit closed the broader Graph corner-pods and overlay-drawer reset pair; the Stage 440 audit closed the broader Home unified-workbench reset pair; the Stage 442 audit closed the broader Graph minimal-launcher and compact-path-rail reset pair; the Stage 444 audit closed the broader Home single-board pinned-cluster reset pair; the Stage 446 audit closed the broader Graph working-control hierarchy reset pair; the Stage 448 audit closed the broader Home organizer-owned navigation reset pair; the Stage 450 audit closed the broader Graph corner-actions and drawer-hierarchy reset pair; the Stage 452 audit closed the broader Home compact reopen rail and board-top hierarchy reset pair; the Stage 454 audit closed the broader Graph settings-sidebar and search-navigation reset pair; the Stage 456 audit closed the broader Home direct-board and organizer-hierarchy reset pair; the Stage 458 audit closed the broader Graph node-language and hover-hierarchy reset pair; the Stage 460 audit closed the broader Home inline-reopen-strip and board-dominant workspace reset pair; the Stage 462 audit closed the broader Graph bottom-bar and drawer workflow reset pair; the Stage 464 audit closed the broader Home organizer-deck and direct-results-sheet reset pair; the Stage 466 audit closed the broader Graph settings-panel and view-controls reset pair; the Stage 468 audit closed the broader Home organizer sorting and board-view reset pair; the Stage 470 audit closed the broader Graph focus-mode and drawer-exploration reset pair; the Stage 472 audit closed the broader Home organizer tree-branch navigation reset pair; the Stage 474 audit closed the broader Graph multi-select path-exploration reset pair; the Stage 476 audit closed the broader Home collection-lens and organizer-model reset pair; the Stage 478 audit closed the broader Graph presets, timeline, and filter-customization reset pair; the Stage 480 audit closed the broader Home overview-board and group-drill-in reset pair; the Stage 482 audit closed the broader Graph navigation-controls and locked-layout reset pair; the Stage 484 audit closed the broader Home manual organizer ordering and selection reset pair; the Stage 486 audit closed the broader Graph color-groups, live-legend, and resizable-settings reset pair; the Stage 488 audit closed the broader Home drag-drop organizer-workbench reset pair; the Stage 490 audit closed the broader Graph saved-view presets workflow reset pair; the Stage 492 audit closed the broader Home resizable organizer rail reset pair; and the Stage 494 audit closed the broader Graph card-drawer and connection-follow reset pair. The Stage 496 audit then closed the broader Home custom collection management reset pair and left Graph, Home, and original-only Reader as refreshed parity baselines again. Stage 497 is a repo closeout and publish plan, not a new product slice: use it to commit the full backlog, rerun the last green validations, promote the branch to main, and prune stale feature refs before reopening product work. Audits verify regressions and evidence; they do not automatically reopen cross-surface queue hopping. docs/exec_plans/active/ may temporarily hold the latest checkpoint pair and older hold-state plans; use docs/ROADMAP_ANCHOR.md, agent.md, and docs/assistant/INDEX.md to identify the current checkpoint."
  }
}
