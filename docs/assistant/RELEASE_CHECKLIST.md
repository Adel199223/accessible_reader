# Release Checklist

- keep product scope unchanged unless the user explicitly asked for product changes
- rerun the targeted frontend/backend/extension checks for touched scope
- rerun harness profile preview when harness files changed
- confirm roadmap/anchor/runbook files still agree on the active checkpoint
- confirm browser companion behavior still matches the context-only contract when extension-facing files changed
- keep push explicit
