# Assistant App Knowledge

Bridge summary only. Canonical project status and policy live in `BUILD_BRIEF.md`, `docs/ROADMAP.md`, `docs/ROADMAP_ANCHOR.md`, and the active ExecPlan.

## Snapshot
- Local-first Recall workspace for Windows 11, with the repo and toolchain living in WSL.
- Frontend: React + Vite + TypeScript.
- Backend: FastAPI + Python 3.11.
- Storage: SQLite with FTS5.

## Current Product Shape
- Imports pasted text, public article webpage URLs, and local TXT, Markdown, HTML, DOCX, and text-based PDF files.
- Stores a small local saved-source collection with search, reopen support, settings, and reading-progress memory.
- Stores webpage imports as local HTML snapshots and reopens them from the saved snapshot rather than refetching live.
- Uses `Recall` as the product shell with `Reader` presented as a shared workspace section rather than a sibling app.
- Uses a collection-first Recall shell with a slimmer left rail, slimmer top bar, primary canvas, and lighter browse chrome by default.
- Uses a browse-first Home landing with a compact inline utility/search header, one flatter lead reopen row without an extra visible first-row cue, nearby reopen support immediately underneath, a lighter same-section continuation flow, a slightly fuller visible continuation tail before the reveal footer row, search, add-source access, and a lightweight selective-landing flow instead of auto-opening focused source mode on populated workspaces, while browse-mode Study now uses a centered review surface with toolbar-level utility and a higher main review card landing.
- Keeps global workspace navigation centered on `Home`, `Graph`, `Study`, `Notes`, and `Reader`.
- Uses a compact focused-source strip during active source work, and focused `Notes`, `Graph`, and `Study` now keep embedded Reader content as the steady primary pane.
- Uses one clear `Add content` dialog with grouped `Paste text`, `Web page`, and `Choose file` modes.
- Uses a graph-first browse-mode `Graph` surface with a dominant canvas, a lighter browse strip, and one attached inspect tray for grounded evidence and grouped continuation, and a browse-mode `Study` surface with a centered review/start frame, toolbar-level queue utility, a quieter pre-answer review-session summary ahead of the prompt, and source-evidence-first Reader reopen.
- Uses one shared workspace-search session across the shell `Search` dialog and the Home search panel.
- Reopens the last document and reading mode from browser-local storage, with backend last-session fallback when local state is missing.
- Supports `Original`, `Reflowed`, `Simplified`, and `Summary` document modes.
- Keeps `Original` and `Reflowed` local and deterministic.
- Wide desktop `Reader` now uses a text-first reading deck with one calmer control ribbon and a docked source/notes support flow, and focused/narrow Reader inherits that same hierarchy.
- Uses browser-native speech with sentence highlighting as the shipped read-aloud path.
- Uses the shared `workspace.db` backend storage with reader-compatible routes kept stable.
- Includes Recall keyword search, hybrid retrieval, graph review, study cards, Markdown export, source-linked notes, MV3 browser-context resurfacing, browser note capture, portability/export groundwork, and integrity/benchmark tooling.
- Current UI execution mode is now a reopened priority queue: the March 19, 2026 Stage 375 audit moved `Reader` out of the active slot after the Reader finish pass, put `Notes` into the active slot, kept `Graph` and `Home` as refreshed regression baselines beside Reader, and kept `Study` parked again until the user explicitly reprioritizes it.

## Active Constraints
- Windows Edge is the primary browser validation target.
- Run repo commands through WSL; Windows-side UNC `npm`/`node` calls are less reliable than `wsl.exe bash -lc ...`, except for the repo-owned Windows Edge screenshot harnesses.
- Local TTS is deferred and should remain `coming soon`.
- AI is opt-in only and limited to `Simplify` and `Summary`.
- Stage 15 is complete: Library and Notes now use denser collection rails, tighter detail panels, and a repo-owned real Edge collection-density smoke harness.
- Stage 16 is complete: Graph and Study now use denser review surfaces, evidence-first detail panels, and a repo-owned real Edge graph/study smoke harness.
- Stage 17 is complete: the UX audit confirmed the next bottleneck is workspace continuity rather than another generic density pass.
- Stage 18 is complete: Library, Notes, Graph, and Study continuity now survive Reader handoff, browser back, and search-backed landings, and the repo-owned real Edge continuity smoke is green.
- Stage 19 is complete: the shared shell now surfaces current context and bounded recent work, and the repo-owned real Edge shell-context smoke is green.
- Stage 20 is complete: the shared shell dock now compresses by section, Reader no longer repeats a dedicated current-source block, Notes detail is lighter, and the repo-owned real Edge context-compression smoke is green.
- Stage 21 is complete: the UX audit identified search continuity and duplicated search surfaces as the highest-friction remaining workflow break.
- Stage 22 is complete: shell Search and Library search now share one remembered search session with grouped results, focused-result actions, and a repo-owned real Edge continuity smoke.
- Stage 23 is complete: the post-search UX audit confirmed that saved-note work is still too detached from active reading.
- Stage 24 is complete: Reader now keeps selected saved-note editing, deletion, Graph promotion, and Study promotion beside the source, with a repo-owned real Edge notebook-adjacency smoke green.
- Stage 25 is complete: the audit confirmed the next workflow break is source fragmentation across Library, Reader, Notes, Graph, and Study rather than notebook adjacency.
- Stage 26 is complete: one selected source now keeps a shared source-workspace frame with nearby Overview, Reader, Notes, Graph, and Study handoffs across Recall and Reader, and the repo-owned real Edge source-workspace smoke is green.
- Stage 27 is complete: the audit confirmed the remaining bottleneck is that source tabs still land inside section-first layouts with duplicated detail panels and always-visible collection rails.
- Stage 28 is complete: the source workspace now has a stronger Overview plus lighter contextual browse drawers for Library, Notes, Graph, and Study during source-focused work, and the repo-owned real Edge shared-source-pane smoke is green.
- Stage 29 is complete: the audit confirmed the remaining bottleneck is no longer repeated source-local detail, but that active source work still sits under dashboard-level shell chrome instead of taking over the workspace.
- Stage 30 is complete: active source work first moved above generic shell chrome and established the source-focused shell mode that later slices refined.
- Stage 31 is complete: the UX audit confirmed shell stacking is no longer the main issue, and the next bottleneck is still hard switching between full-surface source tools.
- Stage 32 is complete: focused `Notes`, `Graph`, and `Study` now keep one source visible in a split workspace, and the repo-owned real Edge Stage 32 smoke is green.
- Stage 33 is complete: the UX audit confirmed the split workspace still anchors too often around `Source overview` instead of the live source itself.
- Stage 34 is complete: focused `Notes`, `Graph`, and `Study` now keep embedded Reader content as the steady pane, with evidence and anchor actions retargeting in place.
- Stage 35 is complete: default Recall now uses the collection-first shell with a rail, slim top bar, compact focused-source strip, and lighter utility dock.
- Stage 36 is complete: the audit confirmed the main remaining break is not focused split work, but that populated `/recall` still auto-enters focused source mode too eagerly.
- Stage 37 is complete: populated `/recall` now stays browse-first by default, and default Library now uses a calmer source-card landing with inline resume instead of opening focused source chrome immediately.
- Stage 38 is complete: the audit confirmed the next bottleneck is visual hierarchy and density, and it corrected the user-reported dark-on-dark landing text plus the broken high-contrast `New` button while capturing fresh live screenshots.
- Stage 39 is complete: the landing now uses calmer, wider source cards with clearer hierarchy, the Library top bar repeats less chrome, and focused Library no longer renders inline `Search workspace`.
- Stage 40 is complete: the audit is now benchmark-driven, using the user-provided Recall screenshots plus official Recall docs/blog/changelog references and fresh localhost captures recorded in `docs/ux/recall_benchmark_matrix.md`.
- Stage 41 is complete: the shared shell is calmer, Library now uses a two-zone sidebar + collection canvas, the add-source flow now groups explicit import modes, and browse-mode Graph/Study frame their main task more intentionally.
- Stage 42 is complete: the audit confirmed the shared shell direction is now right, but the largest remaining benchmark miss is the populated Library/home surface; Add Content is close enough to ride with that next slice, while Graph and Study stay queued behind it.
- Stage 43 is complete: Library now groups recent material into clearer sections, older sources reopen from lighter rows, and the add-content dialog now uses one clear heading.
- Stage 44 is complete: the audit confirmed Graph browse mode as the stronger remaining benchmark mismatch and locked the user-requested `Home` terminology cleanup into the next bounded implementation slice.
- Stage 45 is complete: the shared shell now uses `Home` instead of `Library` in the user-facing navigation, and browse-mode Graph now leads with a graph-first canvas plus lighter support chrome.
- Stage 46 is complete: the audit confirmed that Study is now the clearest remaining top-level benchmark mismatch, while Home is secondary and Graph is materially closer after Stage 45.
- Stage 47 is complete: browse-mode Study now centers the review/start flow, demotes the queue chrome, and keeps source evidence plus Reader reopen secondary to the main review task.
- Stage 48 is complete: the audit confirmed that Study is materially closer after Stage 47, that Graph remains stable, and that Home is once again the clearest remaining top-level benchmark mismatch.
- Stage 49 is complete: Home now uses a lighter sidebar snapshot, a shorter default landing, and explicit expansion controls for larger recency groups while Graph, Study, and focused Study remain stable.
- Stage 50 is complete: the audit confirmed that Home is materially calmer after Stage 49, that Graph remains stable, and that Study is once again the clearest remaining top-level benchmark mismatch.
- Stage 51 is complete: browse-mode Study now uses a lighter queue-control rail, a persistent active-card summary, explicit full-queue reveal controls, and a shorter review-stage support card while Home, Graph, and focused Study stay stable.
- Stage 52 is complete: the audit confirmed that Stage 51 materially improved Study, but that Study is still the clearest remaining mismatch because the queue rail still opens too heavy by default and the stage shell still competes with the main review card.
- Stage 53 is complete: browse-mode Study now lands with queue support collapsed by default, the standalone review-stage shell is gone in favor of inline guidance, and the fresh Stage 53 captures keep Home, Graph, and focused Study stable.
- Stage 54 is complete: the audit confirmed that Stage 53 materially improved Study, but that Study is still the clearest remaining mismatch because the collapsed queue summary and review-card header still read heavier than the benchmark direction wants.
- Stage 55 is complete: browse-mode Study now uses a flatter collapsed support rail and a shorter review header, and the fresh Stage 55 captures keep Home, Graph, and focused Study stable.
- Stage 56 is complete: the audit confirmed that Stage 55 materially improved Study, but that Study is still the clearest remaining mismatch because the support rail still feels boxed and the top-right Reader/journey utility cluster still reads too prominent.
- Stage 57 is complete: browse-mode Study now uses a lighter, more utility-first collapsed rail and a calmer browse-mode review header, and the fresh Stage 57 captures keep Home, Graph, and focused Study stable.
- Stage 58 is complete: the audit confirmed that Stage 57 materially improved Study, but that Study still leads the remaining mismatch list because the lower `Source evidence` block and full-width rating footer still read too heavy.
- Stage 59 is complete: browse-mode Study now uses a lighter lower support stack, a smaller evidence reopen treatment, and a quiet pre-reveal rating placeholder, and the fresh Stage 59 captures keep Home, Graph, and focused Study stable.
- Stage 60 is complete: the audit confirmed that Stage 59 materially improved Study, but that Study still leads the remaining mismatch list because the queue rail still reads boxed and the pre-reveal evidence section still occupies too much space.
- Stage 61 is complete: browse-mode Study now uses a compact `Grounding ready` pre-reveal summary and a visibly softer queue rail, and the fresh Stage 61 captures keep Home, Graph, and focused Study stable.
- Stage 62 is complete: the audit confirmed that Study still leads the remaining mismatch list, but now mainly because browse mode still reads like a dashboard page with a persistent queue summary and extra review-card support chrome rather than a singular quiz/review session.
- Stage 63 is complete: browse-mode Study now uses a narrower, lighter session dock plus a compact review-session summary so the prompt owns the page sooner, and the fresh Stage 63 captures keep Home, Graph, and focused Study stable.
- Stage 64 is complete: the audit confirmed that Study is materially closer after Stage 63 and no longer the clearest blocker, while Home now leads the remaining mismatch list because its support rail and recent-list stack still read too archive-like.
- Stage 65 is complete: Home now uses one quieter utility dock and a flatter grouped recent-source list, and the fresh Stage 65 captures keep Graph, Study, and focused Study stable while materially calming the landing.
- Stage 66 is complete: the audit confirmed that Home is materially calmer and no longer the clearest blocker, while Study again leads the remaining mismatch list because its session dock and pre-answer review header still read too heavy.
- Stage 67 is complete: browse-mode Study now uses a narrower session dock, quieter refresh utility, shorter pre-answer summary copy, and lighter flow chrome, and the fresh Stage 67 captures keep Home, Graph, and focused Study stable while materially calming the review surface.
- Stage 68 is complete: the audit confirmed that Stage 67 materially improved Study, but that Study still leads the remaining mismatch list because the collapsed dock summary and pre-reveal `Grounding ready` strip still add extra chrome.
- Stage 69 is complete: browse-mode Study now drops the extra total/review chrome from the collapsed dock summary and uses a slimmer inline `Grounding ready` utility row, and the fresh Stage 69 captures keep Home, Graph, and focused Study stable while materially calming the remaining Study support chrome.
- Stage 70 is complete: the audit confirmed that Stage 69 materially improved Study and that Study is no longer the clearest blocker, while Home again leads the remaining mismatch list because the populated landing still feels too much like a large archive wall with excess header/canvas space.
- Stage 71 is complete: Home now uses a tighter landing header/canvas transition, flatter featured reopen cards, and an inline featured-section reveal, and the fresh Stage 71 captures keep Graph, Study, and focused Study stable while materially calming the landing.
- Stage 72 is complete: the audit confirmed that Stage 71 materially improved Home and that Home is no longer the clearest blocker, while Study again leads the remaining mismatch list because the browse-mode session rail and pre-answer review header still read too dashboard-like.
- Stage 73 is complete: browse-mode Study now uses a shorter utility-like session rail, a flatter `Review` context row, and a quieter in-flow `Grounded` support row, and the fresh Stage 73 captures keep Home, Graph, and focused Study stable while materially calming Study.
- Stage 74 is complete: the audit confirmed that Stage 73 materially improved Study and that Study is no longer the clearest blocker, while Home again leads the remaining mismatch list because the utility rail and featured reopen row still read too sidebar/archive-like.
- Stage 75 is complete: Home now uses a lighter utility rail plus one deliberate featured-reopen spotlight with quieter nearby rows, and the fresh Stage 75 captures keep Graph, Study, and focused Study stable while materially calming the landing.
- Stage 76 is complete: the audit confirmed that Stage 75 materially improved Home and removed the utility rail as the main blocker, but that Home still leads the remaining mismatch list because the spotlight card is too tall and the secondary reopen stack is still too boxed.
- Stage 77 is complete: Home now uses a shorter featured spotlight and a denser nearby reopen stack, and the fresh Stage 77 captures keep Graph, Study, and focused Study stable while materially calming the landing again.
- Stage 78 is complete: the audit confirmed that Stage 77 materially improved Home enough that Home is no longer the clearest blocker, while Study now leads the remaining mismatch list because the browse-mode `Session` dock and pre-answer review meta still over-frame the task.
- Stage 79 is complete: Study now uses a lighter collapsed `Session` dock and a quieter pre-answer review strip, and the fresh Stage 79 captures keep Home, Graph, and focused Study stable while materially calming the browse-mode review surface.
- Stage 80 is complete: the audit confirmed that Stage 79 materially improved Study and that Study is no longer the clearest blocker, while Home now leads the remaining mismatch list because the featured band still feels too staged and the landing still carries too much empty canvas.
- Stage 81 is complete: Home now uses a flatter featured band, fewer default nearby reopen rows, and a tighter landing rhythm, and the fresh Stage 81 captures keep Study, Graph, and focused Study stable while materially calming the Home landing.
- Stage 82 is complete: the audit confirmed that Home is no longer the clearest blocker, while Study now leads the remaining mismatch list because the browse-mode `Session` rail and pre-answer `Grounded` row still frame the review task too heavily.
- Stage 83 is complete: Study now uses a quieter collapsed `Session` rail and a lighter pre-answer `Grounded` row, and the fresh Stage 83 captures keep Home, Graph, and focused Study stable while materially calming the Study browse surface.
- Stage 84 is complete: the audit confirmed that Study is no longer the clearest blocker, while Home now leads the remaining mismatch list again because the landing still ends too early and feels too much like one isolated featured band.
- Stage 85 is complete: Home now extends the featured reopen band into a quieter same-section `Keep going` continuation so the lower canvas fills without bringing back the old archive-wall feel.
- Stage 86 is complete: the audit confirmed that Home is no longer the clearest blocker, while Study now leads the remaining mismatch list again because the browse-mode `Session` dock and pre-answer review framing still read too sidebar-like.
- Stage 87 is complete: browse-mode Study now uses a slimmer collapsed session dock, a shorter review entry frame, and a shorter evidence-label-only grounding row so the prompt and reveal flow begin sooner.
- Stage 88 is complete: the audit confirmed that Study is no longer the clearest blocker, while Home now leads the remaining mismatch list again because the utility/search rail and upper landing setup still read heavier than the benchmark direction wants.
- Stage 89 is complete: Home now uses a flatter utility/search rail and a tighter header-to-reopen handoff, and the fresh Stage 89 captures keep Study, Graph, and focused Study stable while materially calming the landing.
- Stage 90 is complete: the audit confirmed that Home still remains the clearest blocker after Stage 89 because the split top-of-page header and separate first-section intro still keep the first reopen point too low and too staged.
- Stage 91 is complete: Home now merges the no-resume `Saved sources` header into the first reopen section, starts the featured reopen flow higher on the page, and keeps Study, Graph, and focused Study stable in fresh Stage 91 artifacts.
- Stage 92 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 91, and that Study now leads again because the browse-mode `Session` dock and pre-answer review header still frame the task too heavily.
- Stage 93 is complete: Study now uses a quieter collapsed `Session` dock and a shorter pre-answer review strip, and the fresh Stage 93 captures keep Home, Graph, and focused Study stable while materially calming the browse-mode review flow.
- Stage 94 is complete: the audit confirmed that Study is no longer the clearest blocker after Stage 93, while Home now leads again because the left `Find later` utility/search area and the `Saved sources` intro/setup still stage the landing too heavily.
- Stage 95 is complete: Home now uses a quieter `Find later` utility/search area and a shorter `Saved sources` intro, and the fresh Stage 95 captures keep Study, Graph, and focused Study stable while materially calming the landing.
- Stage 96 is complete: the audit confirmed that Home still remains the clearest blocker after Stage 95 because the left support still reads too much like a dedicated column and the `Saved sources` heading/action chrome still stages the landing before the featured reopen flow.
- Stage 97 is complete: Home now uses a quieter utility/search dock with in-rail `Add source`, the merged `Saved sources` row no longer places chip/action chrome above the first reopen card, and the grouped reveal now sits below the initial reopen flow while Study, Graph, and focused Study stay stable in the fresh Stage 97 captures.
- Stage 98 is complete: the audit confirmed that Stage 97 materially improved Home, but that Home still remains the clearest blocker because the support dock still reads too much like a standing secondary column and the `Saved sources` preamble still stages the landing before the featured reopen flow.
- Stage 99 is complete: Home now uses quieter support/search copy and a shorter merged saved-sources preamble, and the fresh Stage 99 captures materially calmed the Home landing while keeping Study, Graph, and focused Study stable.
- Stage 100 is complete: the audit confirmed that Stage 99 materially improved Home, but that Home still remains the clearest blocker because the support/search area still reads like a standing dock and the merged saved-sources setup still stages the landing before the first featured reopen flow.
- Stage 101 is complete: Home now uses an inline utility header instead of the old side dock on the no-resume landing, the saved-sources preamble above the first featured reopen flow is gone, and the fresh Stage 101 captures materially calm Home while keeping Study, Graph, and focused Study stable.
- Stage 102 is complete: the audit confirmed that Stage 101 materially improved Home, but that Home still remains the clearest blocker because the upper landing still reads as split staged zones, with isolated Home meta on the left and separate search/add utilities on the right leaving too much empty canvas before `Start here`.
- Stage 103 is complete: Home now uses a compact heading-and-snapshot row instead of a mini hero plus a separate utility block, the inline search/add treatment is quieter, and the fresh Stage 103 captures keep Study, Graph, and focused Study stable while materially tightening the landing.
- Stage 104 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 103, and that Study now leads again because the browse-mode `Session` rail still reads too sidebar-like and the review flow still sits inside too much empty dashboard canvas.
- Stage 105 is complete: browse-mode Study now uses a lightweight top support strip instead of a standing left sidebar, the review card owns more of the page in the fresh Stage 105 captures, and Home, Graph, and focused Study stayed stable.
- Stage 106 is complete: the audit confirmed that Study is no longer the clearest blocker after Stage 105, and that Home now leads again because the landing still reads too much like one staged featured band above a quieter archive-style lower stack.
- Stage 107 is complete: Home now uses a lighter continuation flow below `Keep going`, so the lower landing feels more connected to `Start here` instead of reading like a separate archive-style list block, while Study, Graph, and focused Study stayed stable.
- Stage 108 is complete: the audit confirmed that Home still remains the clearest blocker after Stage 107, but that the lower continuation flow is no longer the main issue; the remaining weight now sits in the oversized `Start here` spotlight and the upper-right utility/search row.
- Stage 109 is complete: Home now uses a tighter `Start here` spotlight and a quieter inline utility/search row, and the fresh Stage 109 captures keep Study, Graph, and focused Study stable while materially calming the Home opening.
- Stage 110 is complete: the audit confirmed that Home still remains the clearest blocker after Stage 109 because the opening still reads as a split stage, with the featured spotlight separated from nearby/support reopen utility and too much setup canvas before the continuation flow.
- Stage 111 is complete: Home now uses a more continuous opening flow with nearby reopen support directly under the featured spotlight, and the fresh Stage 111 captures keep Study, Graph, and focused Study stable while materially calming the Home opening.
- Stage 112 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 111, and that Study now leads again because the browse-mode review still sits inside too much empty dashboard canvas with a support strip that over-frames the task.
- Stage 113 is complete: browse-mode Study now demotes the remaining support strip into a lighter in-flow utility row and lifts the review card higher into the canvas, and the fresh Stage 113 captures keep Home, Graph, and focused Study stable while materially calming the Study surface.
- Stage 114 is complete: the audit confirmed that Study is no longer the clearest blocker after Stage 113, and that Home now leads again because the lower continuation rows still feel too boxed and disconnected from the calmer opening flow.
- Stage 115 is complete: Home now uses a tighter `Keep going` handoff and flatter lower reopen rows, and the fresh Stage 115 captures keep Study, Graph, and focused Study stable while materially calming the lower Home canvas.
- Stage 116 is complete: the audit confirmed that Home still remains the clearest blocker after Stage 115 because the lower section still ends too abruptly with an isolated reveal control and too much empty lower canvas.
- Stage 117 is complete: Home now integrates the reveal control into the continuation flow, the lower landing ends more intentionally in the fresh Stage 117 captures, and Study, Graph, and focused Study stayed stable.
- Stage 118 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 117, and that Study now leads again because the browse-mode support strip and empty review canvas still frame the task too much.
- Stage 119 is complete: browse-mode Study now keeps the collapsed `Session` support inside the review card as lighter utility, the review card lands higher in the canvas, and the fresh Stage 119 captures keep Home, Graph, and focused Study stable while materially calming Study.
- Stage 120 is complete: the audit confirmed that Study is no longer the clearest blocker after Stage 119, and that Home now leads again because the opening spotlight and nearby handoff still read slightly too staged.
- Stage 121 is complete: Home now uses a lighter `Start here` spotlight plus flatter inline nearby reopen rows, and the fresh Stage 121 captures keep Study, Graph, and focused Study stable while materially calming the Home opening flow.
- Stage 122 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 121, and that Study now leads again because the browse-mode support strip and extra review-canvas space still frame the task too much.
- Stage 123 is complete: browse-mode Study now uses toolbar-level utility instead of a separate support strip, the review card occupies more of the page, and the fresh Stage 123 captures keep Home, Graph, and focused Study stable while materially calming Study.
- Stage 124 is complete: the audit confirmed that Study is no longer the clearest blocker after Stage 123, and that Home now leads again because the lower continuation band and final reveal control still read too much like an abrupt footer ending.
- Stage 125 is complete: Home now uses one softer lower continuation band and a quieter reveal footer control, and the fresh Stage 125 captures keep Study, Graph, and focused Study stable while materially calming the lower landing.
- Stage 126 is complete: the audit confirmed that Home is no longer the clearest blocker after Stage 125, and that Study now leads again because the browse-mode review card still sits beneath a mostly empty top support strip and too much unused top canvas.
- Stage 127 is complete: browse-mode Study no longer renders the ghost top support strip, the review card now lands higher in the canvas, Home/Graph/focused Study stayed stable in fresh real Edge captures, and the first broad `App.test.tsx` run's transient unrelated Notes-promotion failure cleared on isolated rerun plus full-suite rerun.
- Stage 128 is complete: the benchmark audit confirmed that Study no longer leads after the ghost-strip removal, while Home now leads again because the landing still reads as one boxed archive panel with repeated row labels and timestamp/meta weight.
- Stage 129 is complete: Home now uses a flatter collection frame with quieter row labels, shorter timestamp treatment, and lighter support rows, while Study, Graph, and focused Study stayed stable in fresh real Edge captures.
- Stage 130 is complete: the benchmark audit confirmed that Home still leads after the flattening pass because the landing still reads as one broad boxed ledger with a strong right-side date gutter, while Study, Graph, and focused Study stayed stable.
- Stage 131 is complete: Home now uses an unboxed collection zone with row dates collapsed into inline meta, while Study, Graph, and focused Study stayed stable in fresh real Edge captures.
- Stage 132 is complete: the benchmark audit confirmed that Home no longer leads after the Stage 131 pass, while Graph now leads because the support rail and selected-node overlay still over-frame the canvas.
- Stage 133 is complete: Graph now uses a slimmer utility rail, denser quick picks, and a more compact selected-node overlay, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 134 is complete: the benchmark audit confirmed that Graph is materially closer after Stage 133 but still leads because the quick-pick rail remains too card-heavy and the overlay still occupies too much of the stage.
- Stage 135 is complete: Graph now uses a slimmer selector-style quick-pick rail and a smaller default selected-node overlay with one grounded mention visible, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 136 is complete: the benchmark audit confirmed that Graph is materially closer after Stage 135 but still leads because the left rail still opens with stacked metrics and a longer-than-needed quick-pick list.
- Stage 137 is complete: Graph now collapses the left metric stack into one glance summary and shortens the default quick-pick rail, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 138 is complete: the benchmark audit confirmed that Graph still leads after Stage 137, but now mainly because the left selector strip's search, glance, and quick-pick stack still reads like a dedicated support column beside the canvas.
- Stage 139 is complete: Graph now uses a flatter selector strip with an inline glance summary, a lighter search field, and a compact quick-picks kicker row, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 140 is complete: the benchmark audit confirmed that Graph still leads after Stage 139, but now mainly because duplicated intro framing above the canvas and the selected-node overlay still over-frame the browse surface.
- Stage 141 is complete: Graph now collapses the remaining intro shell into a lighter inline surface note and uses a more compact selected-node evidence overlay, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 142 is complete: the benchmark audit confirmed that Graph still leads after Stage 141, but now mainly because the left quick-pick strip and the fully open selected-node overlay still frame the canvas like a support dashboard.
- Stage 143 is complete: Graph now flattens the remaining quick-pick card treatment into a lighter utility list and opens selected-node detail in a smaller default peek state, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 144 is complete: the benchmark audit confirmed that Graph is materially closer after Stage 143 and that Home now leads because the landing still reads too top-heavy and ends in too much empty lower canvas.
- Stage 145 is complete: Home now opens with a split lead-plus-nearby frame and keeps the grouped reveal action inside the lower continuation grid, while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 146 is complete: the benchmark audit confirmed that Home is materially closer after Stage 145 and that Graph now leads because the left selector strip and right selected-node dock still over-frame the canvas.
- Stage 147 is complete: Graph now uses a narrower selector rail, a shorter default quick-pick stack, and a slimmer selected-node detail dock, while Home, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 148 is complete: the benchmark audit confirmed that Graph is materially calmer after Stage 147 and no longer leads, while Home now leads because the landing still reads as a staged split opening cluster with too much empty lower canvas.
- Stage 149 is complete: Home now uses one nearby support row instead of a broader split opening stack on the no-resume landing, and the lower continuation now carries farther before the reveal control while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 150 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 149, but still leads more narrowly because the lower continuation still ends too quickly and the reveal card still reads like a separate boxed endpoint.
- Stage 151 is complete: Home now shows more continuation rows before expansion and uses a lighter footer reveal affordance instead of ending the grid with a boxed reveal card, while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 152 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 151, but still leads more narrowly because the boxed spotlight and separate `Keep going` restart still stage the landing, while Graph, Study, and focused Study stayed lower-priority in the fresh artifacts.
- Stage 153 is complete: Home now uses a flatter overline-led spotlight and a quieter in-flow continuation intro instead of a chip-led boxed lead plus separate `Keep going` restart, while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 154 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 153, but still leads more narrowly because the opening still reads as a split lead-plus-support stage, while Graph, Study, and focused Study stayed lower-priority in the fresh artifacts.
- Stage 155 is complete: Home now uses one calmer stacked opening flow and a lighter nearby support row instead of a split lead-plus-support stage, while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 156 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 155, but still leads more narrowly because the opening still reads like a spotlight row and the continuation still ends with a separate reveal endpoint, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 157 is complete: Home now uses a flatter lead row plus an inline continuation reveal row instead of a boxed spotlight and separate footer endpoint, while Graph, Study, and focused Study stayed stable in fresh real Edge captures.
- Stage 158 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 157, but still leads more narrowly because the opening still reads like a deliberate pair and the lower continuation stays too sparse, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 159 is complete: Home now opens with one lead row and a denser visible continuation before the inline reveal row, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 160 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 159, but still leads more narrowly because the top still reads like a singled-out lead row and the visible continuation still ends too soon, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 161 is complete: Home now starts the merged landing inside the same continuation grid and carries the reopen flow farther before the inline reveal row, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 162 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 161, but still leads more narrowly because the `Start here` kicker and the `Show all ...` reveal row still bracket the grid too strongly, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 163 is complete: Home now demotes the remaining `Start here` emphasis into quieter inline meta and uses a flatter footer reveal row, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 164 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 163, but still leads more narrowly because the first cell still carries an extra lead cue and the footer reveal still splits attention across the lower edge, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 165 is complete: Home now removes the extra visible first-row cue and uses one calmer reveal-footer continuation line, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 166 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 165, but still leads more narrowly because the landing still ends too soon and leaves too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 167 is complete: Home now carries more visible continuation before the reveal row and fills more of the lower canvas, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 168 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 167, but still leads more narrowly because the reveal row still arrives too early and the landing still leaves too much empty lower canvas after the visible continuation, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 169 is complete: Home now shows slightly more visible continuation before the reveal row and lands that reveal row lower, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 170 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 169, but still leads more narrowly because the visible continuation still ends too soon and the reveal row still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 171 is complete: Home now carries one more visible reopen item before the reveal row and lands that reveal row lower, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 172 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 171, but still leads more narrowly because the visible continuation still ends too soon and the reveal row still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 173 is complete: Home now carries one more visible reopen item through the continuation tail before the reveal footer row and lands that footer lower, while Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 174 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 173, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 175 is complete: Home now shows slightly more visible continuation through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 176 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 175, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 177 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 178 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 177, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 179 is complete: Home now shows slightly more visible continuation through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 180 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 179, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 181 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 182 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 181, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 183 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 184 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 183, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 185 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 186 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 185, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 187 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 188 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 187, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 189 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 190 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 189, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 191 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 192 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 191, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 193 is complete: Home now carries one more visible continuation item through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 194 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 193, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 195 is complete: Home now carries slightly more visible continuation through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 196 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 195, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 197 is complete: Home now carries the visible continuation farther through the landing tail before the reveal footer row, that reveal footer lands lower, and Graph, Study, and focused Study stayed byte-stable in fresh real Edge captures.
- Stage 198 is complete: the benchmark audit confirmed that Home is materially calmer after Stage 197, but still leads more narrowly because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Graph, Study, and focused Study stayed lower-priority in the fresh rerun.
- Stage 199 is complete: Home now carries slightly more visible continuation through the landing tail before the reveal footer row, Study and focused Study stayed byte-stable in fresh real Edge captures, and Graph rerendered without material visual drift on manual review.
- Stage 200 is complete: the benchmark audit confirmed that Home still leads more narrowly after Stage 199 because the visible continuation tail still ends too soon and the reveal footer still lands above too much empty lower canvas, while Home, Graph, and focused Study matched the prior capture set exactly and Study rerendered without material visual drift.
- Stage 201 is complete: Home now carries materially farther through the landing tail before the reveal footer row in the bundled landing-endpoint pass, Graph and focused Study stayed byte-stable in the fresh real Edge captures, and Study rerendered without material visual drift on visual review.
- Stage 202 is complete: the benchmark audit confirmed that Home no longer leads after the bundled Stage 201 landing-endpoint convergence pass, while Home and focused Study matched the Stage 201 captures exactly and Graph plus Study rerendered without material visual drift on visual review; Graph now leads because the left selector rail and right detail dock still bracket the canvas too heavily.
- User-reported responsive regression to revisit later: at narrower desktop window widths, the Recall section rail can reflow into a large top navigation grid/panel that feels materially heavier than the maximized-shell layout.
- Recall section refresh persistence is fixed: `/recall` now keeps the active non-Home section in the URL, so hard refresh restores `Graph`, `Study`, or `Notes` instead of dropping back to `Home`.
- The broad `frontend/src/App.test.tsx` file now completes again as a whole-file WSL Vitest pass; targeted tests plus real Edge screenshot artifacts still lead the UI-validation ladder, but the broad App integration file is trustworthy again for shell and route continuity.
- The last real `App.test.tsx` whole-file stall was not a Vitest mystery: `App.tsx` was recreating shell handoff callbacks on every render, and `ReaderWorkspace` effects depended on those changing props, creating a render/effect loop that only looked like a stuck runner.
- Future work should preserve local-first behavior, routes, note anchors, browser-companion handoff, and reading continuity, but should not preserve the current UI arrangement when a better Recall-quality workflow is available.
- Treat the original Recall app as a directional benchmark for workflow and information hierarchy, not as a pixel-perfect copy target.
- Stage 204 is complete: the Graph audit reran drift-free against Stage 203 and confirmed that Graph still leads because the selector strip and selected-node dock still bracket the canvas like standing support columns.
- Stage 205 is complete: Graph now merges its glance into the selector-strip picker bar, shortens the default quick-pick stack, and uses a smaller inline clue for the default selected-node peek; Home, Study, and focused Study matched Stage 204 exactly.
- Stage 206 is complete: Home, Study, and focused Study matched Stage 205 exactly, and the Graph rerender showed no material visual drift on review; Graph still leads because the selector strip still reads like a utility column and the default selected-node dock still opens with too much header/meta framing.
- Stage 207 is complete: the open Graph selector strip now collapses its visible rail header into one lighter search-plus-toggle utility row, and the default selected-node peek now demotes status/confidence into the summary instead of a stronger header block.
- Stage 208 is complete: the full capture set matched Stage 207 without drift, and Graph still leads because the browse canvas is still bracketed by a standing left selector strip and a persistent right detail peek.
- Stage 209 is complete: Graph now uses a narrower selector strip and a smaller default selected-node peek so the browse canvas keeps more of the page while expanded detail preserves grounded evidence and decision actions.
- Stage 210 is complete: Home and focused Study matched Stage 209 exactly, Study rerendered without material visual drift, and Graph still leads because the selector strip still reads like a standing utility column while the default selected-node peek still brackets the canvas.
- The current highest-priority task is Stage 211: collapse the remaining selector-strip utility stack and soften the default selected-node peek before the next benchmark audit.

## Main Entry Points
- Frontend shell: `frontend/src/App.tsx`
- Speech behavior: `frontend/src/hooks/useSpeech.ts`
- Reader rendering: `frontend/src/components/ReaderSurface.tsx`
- API entrypoint: `backend/app/main.py`
