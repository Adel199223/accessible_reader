# Issue Memory

Keep repeated harness and environment lessons short and concrete.

## Current Reusable Lessons

- run repo commands from WSL, not directly from the UNC path through Windows tooling
- use Windows Edge as the browser truth surface
- keep the extension context-only unless the user explicitly reopens capture/import scope
- keep AI opt-in and limited to `Simplify` and `Summary`
- treat `Reader` generated-content work as locked unless the user explicitly reprioritizes it
