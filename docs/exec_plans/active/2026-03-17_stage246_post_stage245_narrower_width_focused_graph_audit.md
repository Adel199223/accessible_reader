# ExecPlan: Stage 246 Post-Stage-245 Narrower-Width Focused Graph Audit

## Summary
- Rerun the Windows Edge audit after the Stage 245 narrower-width focused `Graph` node-detail header deflation pass.
- Compare refreshed narrower-width focused `Graph` captures against the calmer post-Stage-244 baseline and current benchmark direction.

## Audit Goals
- Confirm that focused `Graph` no longer spends too much of the right support lane on confirm/reject plus selected-node summary chrome before `Mentions` begin at the targeted breakpoint.
- Confirm that Reader remains clearly primary while selected-node continuity, confirm/reject actions, and evidence handoffs remain intact.
- Confirm that the Stage 223 shell correction, Stage 225 Reader gains, Stage 227 strip compression, Stage 229 focused split balance, Stage 237 focused `Notes` gains, Stage 239 focused `Graph` gains, Stage 241 focused `Study` gains, and Stage 243 focused `Graph` gains remain intact while the focused `Graph` node-detail header flattens.

## Planned Validation
- `node scripts/playwright/stage246_post_stage245_narrower_width_focused_graph_audit_edge.mjs`
- compare fresh captures with the Stage 244 baseline and the benchmark matrix direction
- rerun targeted frontend validation if the audit exposes a structural regression
