# ExecPlan: Stage 679 Post-Stage-678 Home Checklist Outline Preview Rebalance Audit

## Summary
- Audit the Stage 678 checklist-outline body-rhythm rebalance on wide-desktop `Home`.
- Confirm the slimmer-rail, wider-body checklist treatment without reopening document-outline work, article-sheet work, note tiers, preview-source routing, or broader Home chrome/layout changes.
- Keep `Graph` and original-only `Reader` as regression surfaces only.

## Scope
- Verify only the bounded Home preview-art changes introduced in Stage 678.
- Confirm preview source continuity for the representative weak `Web`, TXT, and longer structured paste sheet controls.
- Re-check the existing note-tier anchors so the Stage 668-677 preview ladder does not regress.

## Acceptance
- The representative weak Stage 10 `Web` card resolves to `image` / `content-rendered-preview` and keeps the Stage 674 article-sheet signature.
- The representative TXT document card resolves to `image` / `content-rendered-preview` and keeps the Stage 676 document-outline signature.
- The representative longer structured paste sheet control resolves to `image` / `content-rendered-preview` and presents a measurably lighter checklist-outline footprint than the Stage 677 version while still staying more segmented than the TXT document-outline control.
- The sparse Stage11 focus-note and promoted Stage13 summary-note controls keep their established signatures.
- Stage 615 width cadence, Stage 617 board start, `4` visible toolbar controls, `0` visible day-group count nodes, `Graph`, and original-only `Reader` remain stable.

## Validation
- Re-run the Stage 678 validation ladder.
- Record fresh wide-desktop Home captures for the weak `Web`, TXT, structured paste, note-tier anchors, plus regression captures for `Graph` and original-only `Reader`.
