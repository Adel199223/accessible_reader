# ExecPlan: Stage 334 Post-Stage-333 Narrower-Width Focused Graph Audit

## Summary
- Run the narrower-width benchmark audit after Stage 333 to verify whether the focused `Graph` `Node detail` readability rebalance materially improves the live rail at `820x980`.
- Reuse the March 18, 2026 benchmark set so the comparison stays anchored to the same narrow-width focused surfaces that have been guiding the recent dominant-surface tail.
- Confirm that the Stage 333 pass preserves the Stage 223 shell correction, Stage 225 Reader gains, Stage 227 focused-source strip compression, Stage 229 split balance, the Stage 249 focused `Notes` gains, the Stage 251 focused overview gains, the Stage 253 focused `Study` gains, the Stage 263 focused `Notes` drawer-open empty-detail gains, the Stage 281 focused `Graph` gains, the Stage 283 focused `Study` gains, the Stage 285 focused `Graph` gains, the Stage 287 focused `Study` gains, the Stage 289 focused `Graph` gains, the Stage 291 focused `Study` gains, the Stage 293 focused `Graph` gains, the Stage 295 focused `Graph` gains, the Stage 297 focused `Graph` gains, the Stage 299 focused `Graph` gains, the Stage 301 focused `Graph` gains, the Stage 303 focused `Graph` gains, the Stage 305 focused `Graph` gains, the Stage 307 focused `Graph` gains, the Stage 309 focused `Graph` gains, the Stage 311 focused `Graph` gains, the Stage 313 focused `Graph` gains, the Stage 315 focused `Graph` gains, the Stage 317 focused `Graph` gains, the Stage 319 focused `Graph` gains, the Stage 321 focused `Study` gains, the Stage 323 focused `Study` gains, the Stage 325 focused `Study` gains, the Stage 327 focused `Graph` gains, the Stage 329 focused `Graph` gains, and the new Stage 331 focused `Graph` gains.

## Audit Focus
- Focused `Graph` at `820x980`, especially the full `Node detail` rail plus the leading grounded-evidence preview, repeated-source continuation, and the `Relations` follow-on footer beside live reading
- Focused overview, focused `Study`, answer-shown focused `Study`, focused `Notes` drawer-open empty state, and Reader stability after the Stage 333 pass
- Whether the focused `Graph` rail now reads like a calm readable support flow instead of either a boxed destination panel or a too-cramped micro-utility rail

## Captures
- Focused overview narrow top
- Focused `Graph` narrow top
- Focused `Graph` narrow full
- Focused `Graph` node-detail rail narrow top
- Focused `Graph` leading-preview/detail crop if still needed
- Focused `Study` narrow top
- Focused `Study` answer-shown narrow top
- Focused `Notes` drawer-open empty narrow top
- Reader narrow top

## Validation
- Run the Stage 333 Windows Edge harness first and confirm it succeeds
- Run the Stage 334 Windows Edge audit harness against the live localhost app
- Review the capture set against `docs/ux/recall_benchmark_matrix.md`
- Record whether focused `Graph` still leads, whether the blocker has shifted, or whether another surface now becomes the clearer next priority

## Exit Criteria
- The audit clearly states whether Stage 333 succeeded overall
- The audit identifies the next highest-leverage surface or confirms that focused `Graph` still leads
- Any newly observed regression against the retained narrow-width gains is called out explicitly before the next implementation slice is opened
