# ExecPlan: Stage 677 Post-Stage-676 Home Outline Sheet Preview Differentiation Audit

## Summary
- Audit the Stage 676 outline/document-sheet refinement on wide-desktop `Home`.
- Confirm the new checklist-led versus document-led split inside the existing outline-sheet lane without reopening note tiers, article-sheet work, preview-source routing, or broader Home chrome/layout changes.
- Keep `Graph` and original-only `Reader` as regression surfaces only.

## Scope
- Verify only the bounded Home preview-art changes introduced in Stage 676.
- Confirm preview source continuity for the representative weak `Web`, TXT, and longer structured paste sheet controls.
- Re-check the existing note-tier anchors so the Stage 668-675 preview ladder does not regress.

## Acceptance
- The representative weak Stage 10 `Web` card resolves to `image` / `content-rendered-preview` and keeps the Stage 674 article-sheet signature.
- The representative TXT document card resolves to `image` / `content-rendered-preview` and presents a measurably stable document-led outline-sheet signature rather than drifting toward the checklist-biased paste treatment.
- The representative longer structured paste sheet control resolves to `image` / `content-rendered-preview` and keeps a measurably stable checklist-led outline-sheet signature.
- The sparse Stage11 focus-note and promoted Stage13 summary-note controls keep their established bright-panel signatures.
- Stage 615 width cadence, Stage 617 board start, `4` visible toolbar controls, `0` visible day-group count nodes, `Graph`, and original-only `Reader` remain stable.

## Validation
- Re-run the Stage 676 validation ladder.
- Record fresh wide-desktop Home captures for the weak `Web`, TXT, structured paste, note-tier anchors, plus regression captures for `Graph` and original-only `Reader`.
