# ExecPlan: Stage 675 Post-Stage-674 Home Structured Sheet Preview Differentiation Audit

## Summary
- Audit the Stage 674 structured sheet-lane refinement on wide-desktop `Home`.
- Confirm the new article-like versus outline/document-like sheet split without reopening summary-note work, preview-source routing, or broader Home chrome/layout changes.
- Keep `Graph` and original-only `Reader` as regression surfaces only.

## Scope
- Verify only the bounded Home preview-art changes introduced in Stage 674.
- Confirm preview source continuity for the representative weak `Web`, TXT, and longer structured paste sheet controls.
- Re-check the existing note-tier anchors so the Stage 668-673 preview ladder does not regress.

## Acceptance
- The representative weak Stage 10 `Web` card resolves to `image` / `content-rendered-preview` and presents a measurably distinct article-style sheet signature versus the outline-style TXT/document sheet signature.
- The representative TXT document card resolves to `image` / `content-rendered-preview` and keeps the denser outline/document sheet signature.
- The representative longer structured paste sheet control resolves to `image` / `content-rendered-preview` and stays aligned with the outline/document sheet lane.
- The sparse Stage11 focus-note and promoted Stage13 summary-note controls keep their established bright-panel signatures.
- Stage 615 width cadence, Stage 617 board start, `4` visible toolbar controls, `0` visible day-group count nodes, `Graph`, and original-only `Reader` remain stable.

## Validation
- Re-run the Stage 674 validation ladder.
- Record fresh wide-desktop Home captures for the weak `Web`, TXT, structured paste, note-tier anchors, plus regression captures for `Graph` and original-only `Reader`.
