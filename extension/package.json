{
  "name": "accessible-reader-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:debug": "node build.mjs --debug",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "@types/node": "^24.10.1",
    "esbuild": "^0.25.3",
    "typescript": "~5.9.3",
    "vitest": "^4.0.5"
  }
}
