# Agent Runbook

Short runbook for future Codex chats in this repo. Treat `BUILD_BRIEF.md`, `docs/ROADMAP.md`, `docs/ROADMAP_ANCHOR.md`, and the active ExecPlan as canonical project truth.

## Current Checkpoint
- Active checkpoint doc: none; latest completed audit doc is `docs/exec_plans/active/2026-05-09_stage1033_post_stage1032_graph_path_study_questions_audit.md`
- Resume shortcut: `resume after Stage 1033 Graph path Study Questions audit`
- Current benchmark note: the completed Stage 706/707 closeout baseline now sits beneath completed Stage 1032/1033 Graph path Study Questions/gap-build handoffs, completed Stage 1030/1031 Graph path relation practice handoffs, completed Stage 1028/1029 Graph relation practice return loop, completed Stage 1026/1027 Graph relation practice handoffs, completed Stage 1024/1025 relation practice progress signals, completed Stage 1022/1023 relation practice review-session handoff, completed Stage 1020/1021 Home relation practice gap discovery, completed Stage 1018/1019 Source relation practice gap build, completed Stage 1016/1017 Home relation-practice queue lens, completed Stage 1014/1015 related-source practice readiness, completed Stage 1012/1013 Home related-source discovery, completed Stage 1010/1011 source-related Graph resurfacing, completed Stage 1008/1009 source-scoped Graph connection review, completed Stage 1006/1007 Graph connection review queue, completed Stage 1004/1005 Graph gap build queue and return loop, completed Stage 1002/1003 Home Graph-gap Reading queue lens, completed Stage 1000/1001 highlight review Graph connection filters, completed Stage 998/999 highlight review Graph coverage and knowledge handoffs, completed Stage 996/997 highlight review guided sessions, completed Stage 994/995 highlight review bulk triage, completed Stage 992/993 highlight review next actions, completed Stage 990/991 covered-highlight review recap return loop, completed Stage 988/989 multi-card highlight Study coverage selection, completed Stage 986/987 Study review completion highlight-state behavior, completed Stage 984/985 highlight-covered review session handoff, completed Stage 982/983 queue-scoped highlight review, completed Stage 980/981 learning-gap queue filters, completed Stage 978/979 source learning gaps and next actions, completed Stage 976/977 highlight review state and Study coverage, completed Stage 974/975 Library reading queue and highlight-to-Study actions, completed Stage 972/973 Reader highlights/resume/collection inbox, completed Stage 970/971 Collection learning workspaces, completed Stage 968/969 Library collection tree, completed Stage 966/967 Add Content import collections and Pocket ZIP fidelity, completed Stage 964/965 Add Content bulk import queue, completed Stage 962/963 Workspace backup payloads and safe local restore, completed Stage 960/961 Workspace backup preview and restore readiness, completed Stage 958/959 Source learning exports and workspace backup, completed Stage 956/957 Reader-led source quiz launch, and the local Study quiz engine ladder through Stage 954/955.
- Workflow reset: Stage 1032/1033 Graph path Study Questions handoffs are complete above Stage 1030/1031 Graph path relation practice handoffs. Graph Path Finder results now open path-filtered Study Questions for existing coverage, build first missing path practice through the relation-backed Study card seam, and recap with `Study path questions` while preserving `Practice path`, Graph single-connection, and Home/Source relation-practice returns. Choose the next product slice deliberately with a fresh ExecPlan.
- Queued order: none; the Stage 692-707 roadmap is complete.
- Latest scoped surfaces: Home source boards, Home Matches, and source-memory discovery controls; keep Home source memory signals, source-memory continuity, Home personal-notes board/lane/search, source-note promotion semantics in embedded `Notebook`, `Graph`, and `Study`, Reader generated outputs, Add Content, and backend graph APIs as regression surfaces until a new product slice is explicitly opened.

## Read Order
1. `AGENTS.md`
2. `BUILD_BRIEF.md`
3. `docs/ROADMAP.md`
4. `docs/ROADMAP_ANCHOR.md`
5. `docs/ux/recall_benchmark_matrix.md` when the task touches Recall shell or surface UI
6. the latest checkpoint ExecPlan(s) named in `docs/ROADMAP_ANCHOR.md`
7. `docs/assistant/INDEX.md` for harness routing

## Operating Rules
- Work from WSL for repo commands and toolchains, preferably through `wsl.exe bash -lc ...` when starting from Windows-side shells.
- Use Windows Edge as the browser validation target.
- Treat the browser app as the primary surface and the Edge browser companion as a supported secondary surface.
- Keep the canonical repo in WSL for this project; do not migrate it to native Windows as the default fix for transient launcher or service issues.
- When asked to open the app on this machine, use `powershell -ExecutionPolicy Bypass -File .\scripts\open_recall_app.ps1` from the repo root instead of ad hoc `Start-Process msedge`, `cmd /c start`, or direct browser shell commands. The launcher now preflights WSL through `scripts/ensure_wsl_ready.ps1`, reuses or starts the repo-local backend, ensures the built Recall shell exists, and opens Edge through the repo-owned Playwright path.
- The Edge launcher path now prefers the temporary Windows Playwright harness when it is intact and falls back to the healthy Codex runtime Playwright install when that temp harness is only partially populated.
- If the launcher reports that `WslService` is disabled, repair it in an elevated PowerShell with:
  - `Set-Service -Name WslService -StartupType Manual`
  - `Start-Service -Name WslService`
  - `wsl --status`
- On this Windows 11 machine, keep `CCleaner7` background automation disabled unless it is explicitly needed again; repeated local `WslService -> Disabled` events correlated with CCleaner background activity during the Stage 814/815 investigation.
- Keep parsing, storage, search, settings, progress, and reflow local-first.
- Public webpage import is snapshot-based: fetch once, store locally, and reopen from the local snapshot.
- Ship browser-native speech first; local TTS stays `coming soon` until reprioritized.
- AI is opt-in only and limited to `Simplify` and `Summary`.
- Keep the extension context-only unless the user explicitly reopens broader capture/import work.
- Major or multi-file work starts by creating or updating an ExecPlan in `docs/exec_plans/active/`.
- Run targeted validation before broader checks.
- Treat screenshot-based benchmark comparison as required when changing Recall shell or top-level surface UI.
- Wide desktop is the primary benchmark truth for every remaining milestone; focused/narrow adaptation comes second inside the same section milestone.
- Audits are regression gates and evidence snapshots, not permission to drift away from the queued roadmap. After the Stage 692/695 roadmap reset ladder, follow the queued order instead of reopening ad hoc micro-passes unless a catastrophic regression or benchmark contradiction forces a detour.
- In the queued Reader track, UI/UX work may extend across `Original`, `Reflowed`, `Simplified`, and `Summary`, but do not change generated output text, transform logic, cache semantics, generated placeholders, or mode-routing unless the user explicitly reprioritizes generated-content work.
- Every milestone must produce explicit wide-desktop before/after artifacts for the active section and a plain-language statement of what visibly changed.
- Stale temp files, superseded screenshot harness files, dead CSS hooks, and other non-essential generated scaffolding may be deleted when they stop helping; do not let preserving obsolete files slow roadmap work down.
- The stable post-closeout baseline still holds underneath this reopen: Stage 694/695 completed the shared shell reset, Stage 696/697 completed the Home ergonomics follow-through, Stage 698/699 completed the embedded `Notebook` follow-through, Stage 700/701 completed the first broad Reader IA reset, Stage 702/703 completed the generated-mode Reader UX-only refinement and invariant lock while outputs stayed frozen, Stage 704/705 completed the Study review-dashboard reset, and Stage 706/707 completed the final queued cleanup sweep before the roadmap returned to explicit user-directed reopens only.
- Stage 708/709 then intentionally reopened only the global `Add Content` flow: the shell launcher now reads `Add`, the dialog now behaves as a deliberate Recall-style entry surface with clearer mode hierarchy, Home and Reader both open the same route-stable modal, and `Notebook` remains separate from the import path.
- Stage 710/711 then intentionally reopened only `Reader`: wide-desktop Reader now reaches the article sooner with a slimmer attached source strip, a tighter mode-and-transport chrome stack, and a compact support rail that expands only when Source or Notebook work is invoked, while generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 712/713 then intentionally reopened `Reader` again for one bounded follow-through: the at-rest support side now reads as a truer compact context seam with no explanatory copy, only two compact meta chips, hidden visible tab labels at rest, and a materially slimmer dock-to-article ratio while expanded Source and Notebook work stayed intact and generated outputs remained frozen.
- Stage 714/715 then intentionally reopened `Reader` again for one bounded follow-through: the article lane now reads as a centered reading field instead of a broad framed slab, the outer shell stays visually quiet, the compact Source / Notebook seam remains attached, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 716/717 then intentionally reopened `Reader` again for one bounded follow-through: the remaining outer reading-deck shell now stays visually unframed, the pre-article chrome compresses further, the centered article field remains primary, and the compact Source / Notebook seam remains attached while generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 718/719 then intentionally reopened `Reader` again for one bounded follow-through: the attached source strip now reads as a flatter seam instead of a boxed card, the article starts earlier again, the quiet outer stage stays intact, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 720/721 then intentionally reopened `Reader` again for one bounded follow-through: the attached source strip now carries the one visible title above the article, the duplicated Reader-stage title seam is retired, the article starts earlier again, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 722/723 then intentionally reopened `Reader` again for one bounded follow-through: the separate Reader utility row is gone, the visible `View` / `Read aloud` labels are retired at rest, the current view no longer repeats in the metadata chips, the article starts earlier again, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 724/725 then intentionally reopened `Reader` again for one bounded follow-through: the expanded `Source` support state now reads like a lighter attached context rail instead of a second mini workspace, the legacy `Active source` / glance / footer-note chrome is gone, the expanded support ratio is materially slimmer, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 726/727 then intentionally reopened `Reader` again for one bounded follow-through: the lighter Stage 725 Source rail stays intact while the resting metadata row, control band, article field, and compact support seam now sit inside one centered reading band instead of stretching across the full stage, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 728/729 then intentionally reopened `Reader` again for one bounded follow-through: the centered compact Reader deck stays intact while the collapsed Source / Notebook support is now fused into the main attached control band at rest, the separate right-side mini-dock is gone until support is explicitly opened, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 730/731 then intentionally reopened `Reader` again for one bounded follow-through: the centered compact Reader deck and fused support band stay intact while generated-mode unavailable states now stay inside the Reader deck as calm inline placeholders instead of surfacing as top-of-page alert slabs, and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 732/733 then intentionally reopened `Reader` one more time for a bounded end-to-end resume audit and continuity sync: it confirmed the compact reading band and inline support seam remain the real local baseline, Summary empty state stays inline and actionable, Simplified remains optional in the audit and is currently unavailable in the live dataset (`simplifiedViewAvailable: false`), and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 734/735 then intentionally reopened `Reader` one more time for a bounded pre-article lead-in compaction follow-through: the at-rest Reader source-strip shell now stays compact-width with the same `0.773` reading-band footprint as the control ribbon and deck, Source / Notebook work still expands normally when invoked, Simplified remains optional in the audit and is currently unavailable in the live dataset (`simplifiedViewAvailable: false`), and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 736/737 then intentionally reopened `Reader` one more time for a bounded stacked option-row cleanup follow-through: the at-rest `Overview / Reader / Notebook / Graph / Study` row is now demoted into a compact source-destination trigger inside the source strip, the visible `Original / Reflowed / Simplified / Summary` row remains primary, fuller Source / Notebook workspace tabs still return when support is explicitly opened, Simplified remains optional in the audit and is currently unavailable in the live dataset (`simplifiedViewAvailable: false`), and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- Stage 738/739 then intentionally reopened `Reader` one more time for a bounded generated-mode affordance cleanup follow-through: the compact reading band and source-destination trigger stay intact while the visible Reader mode strip now follows the active document's real `available_modes`, unavailable AI modes stop occupying equal-weight pills at rest, Summary remains reachable when supported, Simplified remains optional in the audit and is currently unavailable in the live dataset (`simplifiedViewAvailable: false`), and generated `Original`, `Reflowed`, `Simplified`, and `Summary` outputs remain frozen.
- If `docs/exec_plans/active/` contains both the current implementation plan and a pre-staged next audit plan, use `docs/ROADMAP_ANCHOR.md`, this runbook, and `docs/assistant/INDEX.md` to identify the current one instead of assuming the highest stage number is current.
- Treat `frontend/src/App.test.tsx` as a valid broad shell/route check again. Prefer targeted component tests first, then run the whole file when shell or route continuity changes. If the file ever stalls again, inspect App-level callback identity and `ReaderWorkspace` effect dependencies before assuming Vitest itself is the problem; the last real stall came from callback churn in `App.tsx` creating a render/effect loop.
- Do not push unless the user explicitly asks.

## Routing
- Use `docs/assistant/INDEX.md` for a quick map of the harness docs.
- Use `docs/assistant/workflows/ROADMAP_WORKFLOW.md` for roadmap-driven product work.
- Use `docs/assistant/workflows/EDGE_SPEECH_VALIDATION_WORKFLOW.md` for Edge read-aloud, sentence highlighting, or progress validation.
- Use `docs/assistant/workflows/SESSION_RESUME.md` when the user asks to resume the roadmap or asks where work should restart.
- Use `docs/assistant/workflows/PROJECT_HARNESS_SYNC_WORKFLOW.md` when the user says `implement the template files`, `sync project harness`, `audit project harness`, or `check project harness`.
- Read `docs/assistant/templates/*` only when the user explicitly asks for harness/bootstrap prompt creation, explicit harness sync/audit work, cross-project scaffolding, or a follow-up delta/refinement prompt.
